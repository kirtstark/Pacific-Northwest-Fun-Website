@model IEnumerable<PNFun.Models.BadLink>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>


<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ReportedDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RecreactionSiteID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BadUserID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ReportedDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RecreactionSiteID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BadUserID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
